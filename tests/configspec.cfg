[SETUP]

input_directory = string()
output_directory = string()
file_extension = option('tif', 'mrc', 'mrcs')
GPUs = int_list()
logfile = string()

[general]

pixel_size = float(min=0, max=0.3)
voltage = float(min=0, max=10000)

[motioncor]

[gctf]
